import { lessThen } from 'js--learn-ups/helpers/less-then';
import { module, test } from 'qunit';

module('Unit | Helper | less then');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = lessThen([42]);
  assert.ok(result);
});
