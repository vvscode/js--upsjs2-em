import { firstOfList } from 'js--learn-ups/helpers/first-of-list';
import { module, test } from 'qunit';

module('Unit | Helper | first of list');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = firstOfList([42]);
  assert.ok(result);
});
